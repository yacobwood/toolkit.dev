import type { Metadata } from "next";
export const metadata: Metadata = { title: "Regex Cheat Sheet — Free Online Reference", description: "Searchable regex cheat sheet with patterns, descriptions, and examples. Character classes, quantifiers, lookaround, and more." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
