import type { Metadata } from "next";
export const metadata: Metadata = { title: "Aspect Ratio Calculator — Free Online Tool", description: "Calculate aspect ratios, resize dimensions proportionally, and convert between common presets. Free, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
