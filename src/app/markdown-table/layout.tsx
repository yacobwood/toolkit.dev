import type { Metadata } from "next";
export const metadata: Metadata = { title: "Markdown Table Generator — Free Online Tool", description: "Create markdown tables visually with an interactive editor. Copy the output. Free, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
