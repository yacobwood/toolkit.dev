import type { Metadata } from "next";
export const metadata: Metadata = { title: "SVG Optimizer — Free Online Tool", description: "Minify SVG code by removing metadata, comments, and editor attributes. Free, instant, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
