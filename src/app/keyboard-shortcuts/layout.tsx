import type { Metadata } from "next";
export const metadata: Metadata = { title: "Keyboard Shortcuts Reference — Mac, VS Code, Chrome, Terminal", description: "Searchable keyboard shortcut reference for Mac, VS Code, Chrome, and Terminal. Free, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
