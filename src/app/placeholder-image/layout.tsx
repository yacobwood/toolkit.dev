import type { Metadata } from "next";
export const metadata: Metadata = { title: "Placeholder Image Generator — Free Online Tool", description: "Generate placeholder images with custom size, color, and text. Download as PNG. Free, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
