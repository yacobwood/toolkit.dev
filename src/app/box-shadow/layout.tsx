import type { Metadata } from "next";
export const metadata: Metadata = { title: "CSS Box Shadow Generator — Free Online Tool", description: "Build CSS box shadows visually with sliders. Live preview and copy code. Free, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
