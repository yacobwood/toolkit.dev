import type { Metadata } from "next";
export const metadata: Metadata = { title: "Browser & Device Info — Free Online Tool", description: "View your user agent, screen resolution, timezone, and browser details. Free, instant, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
