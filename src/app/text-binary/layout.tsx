import type { Metadata } from "next";
export const metadata: Metadata = { title: "Text to Binary / ASCII / Hex Converter — Free Online Tool", description: "Convert text to binary, ASCII, hexadecimal, or octal and back. Free, instant, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
