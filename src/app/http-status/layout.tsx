import type { Metadata } from "next";
export const metadata: Metadata = { title: "HTTP Status Codes Reference — Free Online Tool", description: "Complete reference for all HTTP status codes with descriptions. Searchable, categorized. Free, no sign-up." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
